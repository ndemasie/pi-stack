name: CI
on:
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  build_readme:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "💡 Generating the `README.md`."
      - run: |
          source .github/workflows/generate_readme.sh
          README.md <<< $(cat .github/workflows/readme_template.md)
